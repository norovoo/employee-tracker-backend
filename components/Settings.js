import React from 'react';

const Settings = () => {
  return (
    <div className="settings-container">
      <h2>Settings</h2>
      <p>This page is for future settings like email change, password reset, etc.</p>
    </div>
  );
};

export default Settings;